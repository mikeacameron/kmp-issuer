apiVersion: v1
kind: Secret
metadata:
  name: kmpissuer-sample-credentials
type: Opaque
stringData:
  # Generated in Key Manager Plus under Personalize -> API.
  authtoken: "replace-with-the-key-manager-plus-auth-token"
  # Optional: the CA that signed the Key Manager Plus server certificate.
  # ca.crt: |
  #   -----BEGIN CERTIFICATE-----
  #   ...
  #   -----END CERTIFICATE-----
