apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-by-kmpclusterissuer
spec:
  commonName: certificate-by-kmpclusterissuer.example.com
  secretName: certificate-by-kmpclusterissuer
  issuerRef:
    name: kmpclusterissuer-sample
    group: kmp.cert-manager.io
    kind: KMPClusterIssuer
